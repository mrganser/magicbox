var config = {
    http : {
        listenPort: 8080
    },
    mongoDatabase: {
        host: 'host',
        port: 'port',
        database: 'database',
        user: 'user',
        password: 'password'
    }
}

module.exports = config;